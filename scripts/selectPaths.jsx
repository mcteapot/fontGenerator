#target illustrator//Script to autoimate font creationvar sourceFontTemplateDocument = activeDocument;// getActive file which should be "font_template.ai"if(sourceFontTemplateDocument.name === "font_template.ai"){        if(sourceFontTemplateDocument.selection.length > 0) {                while(sourceFontTemplateDocument.selection.length != 0) {            var selectionLen = sourceFontTemplateDocument.selection.length;            $.writeln(selectionLen);            sourceFontTemplateDocument.selection[selectionLen].selected = false;        }                    }        var totalLayers = sourceFontTemplateDocument.layers.length;    var allNames = '';    //move threw layers of main file    for(var i = 0; totalLayers > i; i++) {        var layerName = sourceFontTemplateDocument.layers[i].name;        $.writeln(layerName);        //alert(layerName);        //app.selectedPathPoints        //var layerTarget = sourceFontTemplateDocument.layers[i].selectedPathPoints;        //sourceFontTemplateDocument.selection.add(layerTarget);        var layerTarget = sourceFontTemplateDocument.layers[i].selected;        //sourceFontTemplateDocument.layers.getByName ('c').name.cop        //allNames = (allNames + " " + layerName);             }    app.copy();    //alert(sourceFontTemplateDocument.layers.getByName ('c').name);} else {    //wrong file    alert("wrong file. please run script with file: font_template.ai ", "Errior!", false);}