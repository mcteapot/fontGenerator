#target illustrator//script functionsvar clearSelections = function(sourceDocument) {    if(sourceFontTemplateDocument.selection.length > 0) {        while(sourceFontTemplateDocument.selection.length != 0) {            var selectionLen = sourceFontTemplateDocument.selection.length - 1;            $.writeln(selectionLen);            sourceFontTemplateDocument.selection[selectionLen].selected = false;        }    }         }//main codeif(activeDocument.name === "font_template.ai") {    //global vars    var sourceFontTemplateDocument = activeDocument;        //clear all selections    clearSelections (sourceFontTemplateDocument);        //cycle threw layers in font template    var totalLayers = sourceFontTemplateDocument.layers.length;    for(var i = 0; totalLayers > i; i++) {        var layerName = sourceFontTemplateDocument.layers[i].name;            //alert(layerName);        if(sourceFontTemplateDocument.layers[i].locked === false) {            sourceFontTemplateDocument.layers[i].hasSelectedArtwork = true;            $.writeln(layerName);            $.writeln(sourceFontTemplateDocument.layers[i].pathItems.length);            $.writeln(sourceFontTemplateDocument.layers[i].groupItems.length);                    }                    }    app.copy();    //alert(sourceFontTemplateDocument.layers.getByName ('c').name);} else {    //wrong file    alert("wrong file. please run script with file: font_template.ai ", "Errior!", false);}